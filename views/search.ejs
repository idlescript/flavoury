<!DOCTYPE html>
<html>
  <head>
    <title>Flavoury</title>
    <style>
      body {
        margin: 0;
        font-family: Arial, Helvetica, sans-serif;
        background-color: #4274b9;
      }
      .container {
        margin: 0 auto;
        width: 92%;
        min-width: 320px;
        max-width: 500px;
        margin-top: 9vh;
        padding: 12px;
        background-color: white;
      }
      .content{
        margin-top: 35px;
      }
      .logout-btn{
        padding: 5px;
      }
      .back-btn {
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="title">
        <h2>Search Results:</h2>
      </div>
      <div class="content">
        <form action="/search-recipe" method="get">
          <label for="searchInput">Search:</label>
          <input type="text" name="search" placeholder="Search recipe ..."><br>
          <input type="radio" name="mode" value="personal">Personal
          <input type="radio" name="mode" value="public">Public
          <button type="submit">Search</button>
        </form>
      </div>
      <% if (!searchResult || searchResult.length < 1) { %>
        <% if (typeof searchQuery !== 'undefined' && typeof searchMode !== 'undefined') { %>
          No result.
        <% } %>
      <% } else { %>
        <% for (let data of searchResult) { %>
          <% if (typeof searchQuery !== 'undefined' && typeof searchMode !== 'undefined') { %>
            <a href="/<%= (searchMode === 'public')?'public-recipe':'personal-recipe' %>/<%= data.id %>"><%= data.recipe_title %></a>
          <% } else { %>
            <a href="/public-recipe/<%= data.id %>"><%= data.recipe_title %></a>
          <% } %>

          <!-- If photo exist, it will be shown. Feel free to move this around -->
          <% if (data.photo) { %>
            <img src="/<%= data.photo %>" height="100" width="100">
          <% } %>
          <br><br>
        <% } %>
      <% } %>
      <div class="back-btn"><a href="/personal-cookbook">Back To Personal Cookbook</a></div>
    </div>
  </body>
</html>